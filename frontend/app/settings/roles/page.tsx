import { ComingSoon } from '@/components/ComingSoon';

export default function RolesAccessPage() {
    return <ComingSoon title="Roles & Access" subtitle="Manage workspace permissions and user authentication." icon="badge" />;
}
