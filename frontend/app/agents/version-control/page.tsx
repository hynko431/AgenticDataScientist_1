import { ComingSoon } from '@/components/ComingSoon';

export default function VersionControlPage() {
    return <ComingSoon title="Version Control" subtitle="Track model and pipeline history with git-like precision." icon="history_toggle_off" />;
}
